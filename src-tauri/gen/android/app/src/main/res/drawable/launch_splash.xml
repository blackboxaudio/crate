<?xml version="1.0" encoding="utf-8"?>
<!-- Branded launch background for pre-Android-12 devices (and the window background during the Android 12+
     splash handoff): the app background color with the crate mark centered. On API 31+ the platform splash
     screen (windowSplashScreenBackground/AnimatedIcon in themes.xml) takes over. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:width="180dp"
        android:height="180dp"
        android:drawable="@mipmap/ic_launcher_foreground"
        android:gravity="center" />
</layer-list>
