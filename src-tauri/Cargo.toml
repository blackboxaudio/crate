[package]
name = "crate-app"
version = "0.1.0"
description = "DJ audio library management application"
authors = ["you"]
edition = "2021"

[lib]
name = "crate_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Tauri
tauri = { version = "2", features = ["protocol-asset", "devtools"] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"

# Database
rusqlite = { version = "0.31", features = ["bundled"] }

# Audio
rodio = "0.19"
symphonia = { version = "0.5", features = ["all"] }
lofty = "0.21"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Async
tokio = { version = "1", features = ["full"] }

# Image processing
image = "0.25"

# Hashing
blake3 = "1.5"

# Utilities
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"
log = "0.4"
env_logger = "0.11"
walkdir = "2"
dirs = "5"
sysinfo = "0.31"

