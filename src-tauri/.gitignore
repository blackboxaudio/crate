# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# Schema files for capabilities auto-completion, plus the generated mobile (iOS/Android)
# projects. Mobile is still experimental (M.002), so the gen/ projects are regenerable via
# `tauri ios|android init` and kept out of version control for now. Switch to committing
# gen/apple + gen/android (they ship their own .gitignore excluding Pods/, Externals/, build/)
# once native config — signing, Info.plist, entitlements — needs to persist into release CI.
/gen
