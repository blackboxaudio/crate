{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only plugin permissions: native web-auth (ASWebAuthenticationSession / Custom Tabs) for OAuth sign-in, plus haptics (light impact feedback on tap). NB: neither plugin ships a `default` permission set, so each command is granted explicitly (`web-auth:allow-authenticate`, `haptics:allow-impact-feedback`).",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": ["web-auth:allow-authenticate", "haptics:allow-impact-feedback"]
}
